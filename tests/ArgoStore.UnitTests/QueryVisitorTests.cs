namespace ArgoStore.UnitTests
{
    public class ProviderTests
    {
       // [Fact]
       // public void Test()
       // {
       //     var provider = new ArgoStoreQueryProvider();
       //     ArgoStoreQueryable<TestEntityPerson> source = new ArgoStoreQueryable<TestEntityPerson>(provider);
       //
       //     var result = source.Where(x => x.Active).ToList();
       //
       //     var s = provider.ToString();
       // }
    }
}
